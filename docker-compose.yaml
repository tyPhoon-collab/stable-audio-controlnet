services:
  stable-audio-controlnet:
    build: .
    container_name: stable-audio-controlnet
    volumes:
      - .:/app
      - /Volumes:/Volumes
    environment:
      - PYTHONUNBUFFERED=1
      - HF_TOKEN=${HF_TOKEN}
    tty: true
    stdin_open: true
